{
  "version": 2,
  "outputDirectory": "docs",
  "ignoreCommand": "if ! git rev-parse HEAD^ > /dev/null 2>&1; then exit 1; fi; git diff --quiet HEAD^ HEAD docs/ vercel.json && exit 0 || exit 1"
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Access-Control-Allow-Origin",
          "value": "https://kfc.brmkweb.cn"
        },
        {
          "key": "Access-Control-Allow-Methods",
          "value": "GET, HEAD, OPTIONS"
        },
        {
          "key": "Access-Control-Allow-Headers",
          "value": "*"
        }
      ]
    }
  ],
  "routes": [
    {
      "src": "/[^.]+",
      "dest": "/index.html"
    }
  ]
}